# =============================================================================
# Databricks接続情報（ローカル開発用）
# .env ファイルにコピーして値を設定してください（.envはgitignore済み）
# =============================================================================

# -----------------------------------------------------------------------------
# 【必須】ローカル開発（dev環境）で使用
# -----------------------------------------------------------------------------

# Databricks ワークスペースのURL
DATABRICKS_HOST=https://adb-xxxxx.azuredatabricks.net

# SQL WarehouseのHTTPパス
DATABRICKS_HTTP_PATH=/sql/1.0/warehouses/xxxxx

# Personal Access Token
# Databricks UI → Settings → User Settings → Access Tokens で作成
DATABRICKS_TOKEN=dapi...

# ユーザー名（dev環境のスキーマ名に使用: {USER}_cafe）
USER=your_username

# -----------------------------------------------------------------------------
# 【オプション】ローカルでstg環境をテストする場合のみ
# （通常はGitHub Secretsで設定するため不要）
# -----------------------------------------------------------------------------

# Service Principal（stg用）
# DATABRICKS_CLIENT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
# DATABRICKS_CLIENT_SECRET=xxxxxxxxxxxxxxxxxxxxx

# -----------------------------------------------------------------------------
# 注意事項
# -----------------------------------------------------------------------------
# - stg環境: GitHub Actions で実行（GitHub Secretsから読み込み）
# - prod環境: Databricks Job で実行（Job実行コンテキストから自動取得）
