[project]
name = "dbt-core-databricks-ci"
version = "0.1.0"
description = "dbt project for Databricks with CI/CD (dev/stg/prod)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "dbt-databricks>=1.8.0",
]

[tool.setuptools]
# dbtプロジェクトなので、Pythonパッケージの自動検出を無効化
py-modules = []

